export enum COOKIES_KEYS {
  LOCALIZATION = "LOCALIZATION",
}

export enum LANG {
  AR = "ar",
  EN = "en",
}

export enum ROUTES {
  MAIN = "/",
  CHAT = "/chat",
}

export enum TYPE {
  INCOME = "INCOME",
  EXPENSE = "EXPENSE",
  GOAL = "GOAL",
}
